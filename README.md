# SPX 0DTE Research — static site

Generated by `scripts/build_research_site.py` on 2026-06-04 12:08 PDT (data as of Jun 4, 2026).

- `index.html` — the narrative walkthrough of the research.
- `explore.html` — the interactive policy/regime explorer.

Both pages are self-contained (data is inlined); the only external dependency is
the ECharts CDN. To view:

```bash
python -m http.server 8770      # from inside this folder
# or just open index.html in a browser
```

To rebuild after the research loop updates the data:

```bash
.venv/bin/python scripts/build_research_site.py
```

This site is read-only and regenerated from the same parquet + JSONL artifacts
the research loop writes; do not hand-edit the HTML.
